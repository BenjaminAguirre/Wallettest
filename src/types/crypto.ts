
export interface xPrivXpub {
    xpriv: string;
    xpub: string;
}

export interface externalIdentity {
    privKey: string;
    pubKey: string;
    address: string;
}

export interface keyPair {
    privKey: string;
    pubKey: string;
}
export interface AkashKeyPair {
    privKey: string;
    pubKey: string;
    address: string;
  }
  